name: ahla msa 3la yasser

on: 
  push:
    branches:
      - master



jobs:
    yasser-job:
        runs-on: ubuntu-latest
    
        steps:
            - name: install python and flask
              run: sudo apt install python3 && pip install flask -y

            - name: what is version
              run : python3 --version
            
            - name: test application 
              run: python3 script.py