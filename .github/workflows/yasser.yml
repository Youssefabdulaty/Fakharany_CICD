name: ahla msa 3la yasser

on: 
  workflow_dispatch:
    inputs:
        ENV:
            description: 'Environment name'
            type: string
            required: true
            default: 'production'
        value:
            description: 'A numeric value'
            type: number
            required: true
            default: 50

jobs:
    yasser-job:
        runs-on: ubuntu-latest
        steps:
            - name: test resubale workflow
              uses: ./.github/workflows/reusable.yml
              with:
                ENV: "production"
                value: 50