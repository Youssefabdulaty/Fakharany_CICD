name: all steps

on:
  workflow_dispatch

jobs:
  job_1:
    runs-on: ubuntu-latest

    steps: 
      - uses: actions/checkout@v6.0.0
      - run: |
          npm install
          npm run build
        working-directory: ./temp
        shell: bash

      - run: print %ENV
        
